import {books} from '../data/bookList';
import {preachingSource} from '../data/preachingSource';
import {sermons} from '../data/sermons';

export const LIST = {
    books,
    preachingSource,
    sermons
}